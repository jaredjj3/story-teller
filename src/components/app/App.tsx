import { Layout } from 'antd';
import * as React from 'react';
import { Footer } from './Footer';
import { Header } from './Header';

export const App = () => (
  <Layout>
    <Header />
    <Footer />
  </Layout>
);
